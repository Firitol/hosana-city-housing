# Use legacy peer deps for React Native compatibility
legacy-peer-deps=true

# Save exact versions
save-exact=true

# Registry
registry=https://registry.npmjs.org/

# Audit (optional - can slow down install)
audit=false